apiVersion: v1
kind: Service
metadata:
  name: {{ include "personality-test-frontend.fullname" . }}
  labels:
    {{- include "personality-test-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "personality-test-frontend.selectorLabels" . | nindent 4 }}
