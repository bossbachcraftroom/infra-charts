{{- range .Values.sites }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-svc
  namespace: {{ .namespace }}
spec:
  selector:
    app: {{ $.Release.Name }}
  ports:
    - port: 80
      targetPort: {{ $.Values.defaults.port }}
---
{{- end }}